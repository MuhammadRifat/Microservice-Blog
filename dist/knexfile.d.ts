declare const config: {
    client: string;
    connection: {
        host: string;
        port: string;
        user: string;
        password: string;
        database: string;
    };
};
export default config;
